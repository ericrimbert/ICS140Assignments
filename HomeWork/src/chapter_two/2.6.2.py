# 2.6.2 - Sphere Volume

# import pi from the math module
from math import pi

# variables that set the radius and volume
radius = 1.0
volume = (4.0/3.0) * pi * (radius * radius * radius)

# prints the volume of the sphere
print(f"Volume of the sphere is {volume}.")
